/* script.js */

new fullpage('#fullpage', {
    autoScrolling: true,
    scrollHorizontally: true,
    responsiveWidth: 320, // 768px 이하의 화면에서는 autoScrolling 비활성화
    responsiveHeight: 600,
    responsiveSlides: true
});
